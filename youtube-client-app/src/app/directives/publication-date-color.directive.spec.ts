import { PublicationDateColorDirective } from './publication-date-color.directive';

describe('SetPublicationDateColorDirective', () => {
  it('should create an instance', () => {
    const directive = new PublicationDateColorDirective();
    expect(directive).toBeTruthy();
  });
});
